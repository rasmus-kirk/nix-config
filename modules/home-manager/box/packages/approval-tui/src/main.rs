// approval-tui — host-side gate for sandbox broker requests.
//
// This is a scaffold; the full implementation lands in subsequent commits
// (queue + inotify + signing + audit + Haiku + UI + per-broker dispatch).

fn main() {
    eprintln!(
        "approval-tui scaffold — full implementation pending. \
         Build OK. PID {}",
        std::process::id()
    );
}
